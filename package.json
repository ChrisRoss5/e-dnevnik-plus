{
  "name": "e-dnevnik-plus",
  "private": true,
  "scripts": {
    "prototype:build-targets": "node tools/build-extension-targets.js",
    "prototype:lint:firefox": "npx web-ext lint --source-dir dist-firefox-dev --warnings-as-errors",
    "prototype:run:firefox": "npx web-ext run --source-dir dist-firefox-dev"
  },
  "devDependencies": {
    "web-ext": "^10.1.0"
  }
}
